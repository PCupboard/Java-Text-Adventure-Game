package model.locations;

public class Locations {
    // What does a location need to show the player?
    // The difficulty of the chosen location?
    // Different "areas" in each location. These areas can be the combat area, the shop area etc.
    // These could be stored in a big location class where the player can change their location at will.
    // How should the code differentiate different locations? Each location is its own class?
    // Or
}
